const user = {
  name: "Theo",
  // age: 0,
};

// user.age = 24;

// two ways to fix this:
// one is to add age to the object with a default value of 0
// second is to remove the line user.age = 24;
